# Recovered Project: iftikar-rahaman.netlify.app

This codebase was recovered from the live Netlify deployment after the
original GitHub repository was deleted. It was built with **Vite + React**,
and the deployment method was **Netlify Drop** (a folder dragged directly
onto Netlify, not connected to a Git repo).

## What this means

What you have here is the **production build output**, not your original
editable source code:

- `assets/index-CGPbLzlx.js` — the bundled, minified JavaScript (all your
  React components, logic, and imports compiled into one file)
- `assets/index-CGPbLzlx.beautified.js` — the same file, reformatted for
  readability. Note: minification permanently strips your original variable
  and component names, comments, and file structure. This is readable, but
  it is not your original `.jsx` source.
- `assets/index-BcDHRdQQ.css` — compiled Tailwind CSS v4 output
- `assets/index-BcDHRdQQ.beautified.css` — same, reformatted
- `index.html`, `vite.svg`, and image assets — used as-is

## What's NOT recoverable this way

- Original component file structure (e.g. `src/components/Hero.jsx`)
- Comments, original variable/function names
- Any server-side code, environment variables, or API secrets
- Git history

## Note on project preview images

`project-1`, `project-2`, and `project-3` were filled in using the
screenshots you uploaded (BLOGAR, FitnessPal, BRACU Research Connect).
The mapping between filename and project was guessed from upload order —
double check these match the right project once you preview the site, and
rename if needed.

## To run this as a static site

Any static file server works, e.g.:

```bash
npx serve .
```

Then open the printed local URL.

## Recommendation

If you want your real, editable source back, check whether you have:
- Any local clone/folder on a personal computer
- A code editor's local history feature
- Netlify's "Deploys" page for any other deploys (might have more context)
- The AI tool used to build it (if you used an AI app builder, the original
  chat/session may still hold the source)
